apiVersion: v2
name: automate
description: >-
  MyWork Automate — node-based workflow automation engine (E1-S3).
  Deploys the Go/Gin control-plane API and the Temporal worker, plus
  DEV-ONLY in-cluster Postgres and Temporal for a self-contained kind demo.
type: application
# Chart version — bump on chart changes.
version: 0.1.0
# Application version — the automate app release this chart deploys.
appVersion: "0.1.0"
keywords:
  - workflow
  - automation
  - temporal
maintainers:
  - name: MyWork Automate DevOps
home: https://github.com/mywork/automate
sources:
  - https://github.com/mywork/automate
