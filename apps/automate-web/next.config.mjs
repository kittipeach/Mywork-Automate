/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // The automate UI mounts under /automate within the MyWork admin portal.
  basePath: '/automate',
};

export default nextConfig;
